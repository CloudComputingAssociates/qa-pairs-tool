{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "reverse-prompt.schema.json",
  "title": "ReversePrompt",
  "description": "Schema for prompt-response training pairs stored in the promptme collection",
  "type": "object",
  "required": ["type", "name", "context", "category", "prompt", "action", "next-prompt"],
  "properties": {
    "type": {
      "type": "string",
      "const": "reverse-prompt",
      "description": "Document type discriminator"
    },
    "name": {
      "type": "string",
      "description": "Unique identifier slug (lowercase, hyphenated, e.g. 'welcome-intro') used by next-prompt to link prompts"
    },
    "context": {
      "type": "string",
      "description": "Where using these FAQ's in the app"
    },
    "category": {
      "type": "string",
      "description": "Category from categories collection (dropdown)"
    },
    "subcategory": {
      "type": ["string", "null"],
      "description": "Optional subcategory (free text)"
    },
    "prompt": {
      "type": "string",
      "description": "Prompt the user output"
    },
    "action": {
      "type": "string",
      "description": "Code action or AI prompt for user consideration"
    },
    "next-prompt": {
      "type": "string",
      "description": "Link to follow-on prompt or end"
    },
    "source": {
      "type": ["string", "null"],
      "description": "Origin or reference source for this training pair"
    },
    "attribution": {
      "type": ["string", "null"],
      "description": "Author or contributor attribution"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the document was created"
    }
  }
}
